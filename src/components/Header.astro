<header>
  <h1>
    <span
      style="--start-color:#007CF0; --end-color:#00DFD8; --content: 'Playground.';"
    >
      Playground.
    </span>
    <span
      style="--start-color:#7928CA; --end-color:#FF0080; --content: 'Javascript.'; --animation:a2;"
    >
      Javascript.
    </span>
    <span
      style="--start-color:#FF4D4D; animation-name:a3; --end-color:#F9CB28; --content: 'Fast.'; --animation: a3"
    >
      Fast.
    </span>
  </h1>

  <p>Quickly test your Javascript code ideas, with a real-time result!</p>
</header>

<style>
  p {
    color: #fff;
    font-size: 1.5rem;
    font-weight: 300;
    text-align: center;
  }
  header {
    background: #000;
    display: grid;
    place-content: center;
    width: 100%;
  }
  h1 {
    font-size: 5rem;
    font-weight: 800;
    letter-spacing: -0.06em;
    text-align: center;
    margin: 0 0 16px;
  }
  span {
    color: #fff;
    display: block;
    font-size: 100px;
    line-height: 1.2;
    position: relative;
  }
  span::before {
    animation-name: var(--animation, a);
    animation-duration: 8s;
    animation-iteration-count: infinite;
    background: linear-gradient(90deg, var(--start-color), var(--end-color));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    color: #fff;
    font-size: 100px;
    position: absolute;
    inset: 0;
    content: var(--content);
    opacity: 1;
  }

  @keyframes a {
    0%,
    16.667%,
    100% {
      opacity: 1;
    }

    33.333%,
    83.333% {
      opacity: 0;
    }
  }

  @keyframes a2 {
    0%,
    100% {
      opacity: 0;
    }

    33.333%,
    50% {
      opacity: 1;
    }

    16.667%,
    66.667% {
      opacity: 0;
    }
  }

  @keyframes a3 {
    0%,
    50%,
    100% {
      opacity: 0;
    }

    66.667%,
    83.333% {
      opacity: 1;
    }
  }
</style>
